apiVersion: v1
kind: Service
metadata:
  name: {{ include "accommodation-service.fullname" . }}
  labels:
    {{- include "accommodation-service.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "accommodation-service.selectorLabels" . | nindent 4 }}
