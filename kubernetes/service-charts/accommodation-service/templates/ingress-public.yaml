{{- if .Values.ingress.publicEnabled -}}
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: {{ include "accommodation-service.fullname" . }}-public
  labels:
    {{- include "accommodation-service.labels" . | nindent 4 }}
  annotations:
    {{- with .Values.ingress.annotations }}
    {{- toYaml . | nindent 4 }}
    {{- end }}
    # NO JWT plugin - public browsing (GET only)
    konghq.com/methods: GET
spec:
  ingressClassName: {{ .Values.ingress.className }}
  rules:
  {{- range .Values.ingress.hosts }}
    - host: {{ .host | quote }}
      http:
        paths:
        # Public read-only routes (GET requests only)
        - path: /api/accommodations
          pathType: Prefix
          backend:
            service:
              name: {{ include "accommodation-service.fullname" $ }}
              port:
                number: {{ $.Values.service.port }}
  {{- end }}
{{- end }}
